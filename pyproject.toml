[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-quant-trading-system"
version = "0.2.0"
description = "Research-grade multi-asset AI quantitative trading research system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26,<3",
  "pandas>=2.2,<3",
  "scikit-learn>=1.5,<2",
  "scipy>=1.13,<2",
  "PyYAML>=6.0,<7",
  "yfinance>=0.2.50,<1",
  "matplotlib>=3.9,<4",
  "xgboost>=2.1,<4",
  "requests>=2.31,<3",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9", "ruff>=0.6,<1"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
# scripts/ is on the path so the operational entry points are testable: the
# live data path is the one that cannot be run here and therefore the one that
# most needs a test driven through a fake transport.
pythonpath = ["src", "scripts"]
addopts = "-q"

[tool.ruff]
line-length = 100
